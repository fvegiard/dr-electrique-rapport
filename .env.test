# Supabase credentials for E2E tests
# Copy this file to .env.test.local and fill in your values

# Supabase URL (found in Project Settings > API)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Service Role Key (for cleanup operations - has full access)
# WARNING: Never commit this key or expose it in client-side code
SUPABASE_SERVICE_KEY=your-service-role-key

# Supabase Anon Key (for regular operations)
SUPABASE_ANON_KEY=your-anon-key

# Alternative variable names (for compatibility with existing .env files)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Base URL for tests (optional - defaults to localhost:8002)
# BASE_URL=https://your-netlify-preview-url.netlify.app

# Set to true when running in CI environment
# CI=true
